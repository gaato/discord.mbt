{
  "name": "discord-mbt-workers-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "moon build --target js --release --deny-warn .",
    "test": "vitest run",
    "bundle:check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.2.3",
    "vitest": "4.1.11",
    "wrangler": "4.136.3"
  }
}
