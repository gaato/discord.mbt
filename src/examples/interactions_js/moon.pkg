import {
  "gaato/discord",
  "gaato/discord/model",
  "moonbitlang/async",
  "moonbitlang/async/js_async",
}

// The shared App also builds on native for command registration.

warnings = "-29"

supported_targets = "js + native"

options(
  link: { "js": { "exports": [ "start_signed_interaction" ], "format": "esm" } },
  targets: { "worker.mbt": [ "js" ] },
)
