import {
  "gaato/discord/model",
  "gaato/discord/util",
  "gaato/discord/internal/clock",
}

import {
  "moonbitlang/core/json",
} for "test"

// Discord's public resource name is `member`; keep that getter despite the
// compiler reserving the word for possible future syntax.

warnings = "-35"
