name = "example/discord-bot"

version = "0.1.0"

description = "A minimal Discord bot built with discord.mbt."

source = "src"

preferred_target = "native"

import {
  "gaato/discord@0.2.0",
  "moonbitlang/async@0.22.1",
}
