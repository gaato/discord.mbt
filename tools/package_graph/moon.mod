name = "gaato/discord-package-graph"

version = "0.1.0"

description = "Repo tool: generate docs/packages.mmd and docs/packages.svg from moon.pkg imports."

source = "."

preferred_target = "js"

import {
  "mizchi/moomaid@0.4.0",
  "moonbitlang/x@0.5.1",
}
